export default class RandomUtils {
    /**
     * 生成十六进制字符串
     * @param  length 返回的字符串长度
     * @return       字符范围在 '0123456789ABCDEF' 内得随机字符串
     */
    randomHexadecimal(length?: number): string;
}
